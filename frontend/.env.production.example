# Backend API URL (replace with your deployed backend URL after deploying backend)
VITE_API_URL=https://your-backend.railway.app

# Internal API Key (must match APP_API_KEY in backend .env)
VITE_API_KEY=command_center_secret_key
